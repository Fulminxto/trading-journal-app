import { getCopilotLabels, type CopilotI18nProps } from "@/components/copilot/CopilotI18n";

type Props = CopilotI18nProps & { sessionLocked: boolean; reviewRequired: boolean; };
export default function RecoveryStatusCard({ sessionLocked, reviewRequired, appLanguage }: Props) { const t = getCopilotLabels(appLanguage); const status = sessionLocked ? t.common.locked : reviewRequired ? t.common.required : t.statuses.cleared; return <div className="rounded-[36px] border border-emerald-500/20 bg-emerald-500/10 p-8"><p className="text-sm uppercase tracking-[0.2em] text-emerald-400">{t.components.recoveryStatus.eyebrow}</p><h2 className="mt-3 text-3xl font-black text-white">{status}</h2><p className="mt-5 text-sm leading-relaxed text-gray-300">{sessionLocked ? t.components.recoveryStatus.lockedMessage : reviewRequired ? t.components.recoveryStatus.reviewMessage : t.components.recoveryStatus.clearedMessage}</p></div> }
